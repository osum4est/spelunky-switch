if (msgTimer > 0) msgTimer -= 1;
else msg = global.PressEscapeToReturnToTitle

/*
if (status == GET_FILE_NAME)
{
    if (checkStartPressed() or gamepad.attackPressed)
    {
        if (levelName != "")
        {
            global.firstCustomLevel = levelName;
            scrLoadLevel();
        }
    }
}
*/
/* */
/*  */
